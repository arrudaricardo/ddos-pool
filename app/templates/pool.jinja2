{% extends "base.jinja2" %}

{% block content %}

{# POOL INFO #}

<script src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/2.2.0/socket.io.slim.js"></script>

<div class="container" style="padding:2em">
  <div class="columns">
    <div class="column col-6 col-sm-11">
      <div id='cardInfo' class="card">
    <h1>📇 {{pool.name}}</h1>
    <div class='m-1 d-inline'>🎯 Target:
      <u id='pool-target'>{{pool.target_address}} </u></div>
    <div class='m-1'>💬 Comment:
      <u  id='pool-comment' > {{pool.comment}}
      </u> 
      </div>

    <div class='m-1'>👨🏻‍💻 Number of Attackers: 
       <div class="d-inline"  id='numberAttakers'>{{pool.number_attackers}}</div>
    </div>
    
    <div class="p-2">
    <div> Script Run/min: <input  class="col-3" id='speed'  style="border:none" placeholder='min.' value="1" min="1" type="number"></div>
    <button class="my-2 col-4 d-block btn" id='startButton'>Start Attack</button>
    </div>

</div>
</div>

{# DISPLAY CODE #}
  <div id='displayCodeBox'  class="column col-6 hide-sm col-sm-0 ">
    <pre class="m-0"><strong>Script:</strong>  <code class="JavaScript" id='displayCode' >
    </code></pre> 
  </div>
</div>
</div>

{# CHAT BOX #}
<div id='chatBox' class="container loading">
  <div class="columns">
    <div class="column">

        <textarea readonly id="chat" cols="5" rows="5"></textarea><br><br>
        <input id="text" size="20" placeholder="Enter your message here">
        <button id="sendText" type="submitText" class="btn">Send</button>
        <br><br>
        <button id="disconnectChat" type="submitText" class="btn btn-link">turn chat off</button>

    </div>
  </div>
</div>

{# MODAL #}
<div id="termsModal" class="modal modal-sm" id="modal-id">
  <div class="modal-container">
    <div class="modal-header">
      <div class="modal-title h5">Agreement</div>
    </div>
    <div class="modal-body">
      <div class="content">
        I agree to not use this code for
      </div>
    </div>
    <div class="modal-footer">
      <button onclick="$('.modal').removeClass('active')" class="btn btn-primary">Agree</button>
    </div>
  </div>
</div>

{% endblock %}

{% block script %}
{% if pool.attack_type == 'Custom Code' %}
<script id='attackCode' type=text/javascript>
{{ pool.custom_code|safe }}
</script>



{% else %}
{% set attackType = pool.attack_type %}
<script id="attackCode"> 
  {% include 'attack_script/'+attackType+'.jinja2'%}
</script>
{% endif %}

{#  General Code for pool page    #}
<script type=text/javascript>
  //display code in DOM
  let customCode = $('#attackCode').text();
  $('#displayCode').text(customCode);

   // highlight Block #displayCode
  hljs.highlightBlock(displayCode)

  // set displayCode same height as cardInfo
  //TODO 
  document.getElementById('cardInfo').clientHeight + document.getElementById('displayCodeBox').offsetHeight  - document.getElementById('cardInfo').clientHeight
  $('#displayCode').css('max-height', document.getElementById('cardInfo').clientHeight )
  $(window).on('resize', function(){
  document.getElementById('displayCodeBox').offsetHeight - document.getElementById('cardInfo').clientHeight - document.getElementById('cardInfo').clientHeight
    $('#displayCode').css('max-height', document.getElementById('cardInfo').clientHeight )
  });

  </script>

  <script id='socketIO' type=text/javascript>
  $(window).on('load', function() {
    var socket;
            $(document).ready(function(){

                socket = io.connect('http://' + document.domain + ':' + location.port + '/chat');
                socket.on('connect', function() {
                  $('#chatBox').removeClass('loading')
                    socket.emit('joined', {});
                });
                socket.on('status', function(data) {
                    $('#chat').val($('#chat').val() + '<' + data.msg + '>\n');
                    $('#chat').scrollTop($('#chat')[0].scrollHeight);
                });
                socket.on('message', function(data) {
                    $('#chat').val($('#chat').val() + data.msg + '\n');
                    $('#chat').scrollTop($('#chat')[0].scrollHeight);
                });
                $('#text').keypress(function(e) {
                    var code = e.keyCode || e.which;
                    if (code == 13) {
                        text = $('#text').val();
                        $('#text').val('');
                        socket.emit('text', {msg: text});
                    }
                });
            });
            document.getElementById('disconnectChat').onclick = function () {
              socket.emit('left', {}, function() {
                    socket.disconnect();
                    $('#chatBox').addClass('d-hide')
                });
            }
            document.getElementById('sendText').click = function(){
              text = $('#text').val();
              $('#text').val('');
              socket.emit('text', {msg: text});
            }
});



  </script>

{% endblock %}